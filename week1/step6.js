'use strict';

let wildBirds = [];
console.log('the value of wildBirds could be: (eagle, falcon and owl)');
console.log(wildBirds);
let myPets = [ 'cat', 'dog', 'rabbit' ];
console.log(myPets);
myPets.push('baby pig');
console.log(myPets);

